# 🚨🚨🚨 배포 완전 차단됨 🚨🚨🚨
# 
# 이 워크플로우는 완전히 비활성화되었습니다.
# GitHub Actions에서 실행되지 않습니다.
# 
# 배포는 반드시 수동으로만 진행하세요:
# 1. AWS Console → CodeDeploy 사용
# 2. AWS CLI 사용
# 
# 자동 배포 재활성화가 필요한 경우:
# 1. 이 파일의 주석을 해제하고
# 2. on: workflow_dispatch: 부분을 활성화하세요
#
# 🚫 현재 상태: 모든 자동 배포 차단됨

name: Deploy to EC2 via CodeDeploy (COMPLETELY DISABLED)

# on: 섹션이 없으므로 어떤 이벤트로도 트리거되지 않음

# 모든 배포 로직이 주석 처리됨
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     if: false  # 절대 실행되지 않음
#     steps:
#       - name: Deployment Blocked
#         run: echo "자동 배포가 차단되었습니다"